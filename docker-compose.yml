services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    environment:
      BIND_ADDR: "0.0.0.0:3001"
    ports:
      - "3001:3001"

  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
    ports:
      - "8080:80"
    depends_on:
      - backend